import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    // Server Actions / Route Handlers are stable in App Router; nothing extra needed.
  },
};

export default nextConfig;
