---
phase: "01"
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/clients.ts
  - src/app/api/clients/route.ts
  - src/app/api/clients/[id]/route.ts
  - src/components/nav/ClientSwitcher.tsx
  - src/components/nav/AppNav.tsx
  - src/app/onboarding/page.tsx
  - src/app/clients/[id]/connections/page.tsx
  - src/middleware.ts
  - src/app/clients/route.ts (API list helper, optional)
autonomous: true
requirements: [CLNT-01, CLNT-02, CLNT-03]
must_haves:
  truths:
    - "An authenticated team member can create a client workspace that appears in their client list (CLNT-01)"
    - "An unauthenticated request to create/list clients is rejected (401)"
    - "A request scoped to client Y returns no rows for an account belonging to client X (CLNT-02)"
    - "The DB FK prevents a social_account row without a valid client_id"
    - "Two clients list as distinct entries; editing one does not alter the other; deleting one removes only its scoped resources (CLNT-03)"
    - "The active client is chosen from a nav dropdown, persisted in a server-readable cookie, and scoping is applied server-side (D-01, D-02)"
    - "After login with no clients, the user is sent to onboarding; once a client is selected the default landing is that client's connections list (D-03, D-04)"
  artifacts:
    - path: "src/lib/clients.ts"
      provides: "getActiveClientId() (cookie read) + requireClientScope() server-side scoping helper + resolveActiveClientId() (D-07 stale-cookie-safe resolver)"
      exports: ["getActiveClientId", "requireClientScope", "setActiveClientCookie", "resolveActiveClientId", "listClients", "listConnections"]
    - path: "src/app/api/clients/route.ts"
      provides: "GET list (own clients) / POST create (CLNT-01)"
      exports: ["GET", "POST"]
    - path: "src/app/api/clients/[id]/route.ts"
      provides: "GET/PATCH/DELETE one client (CLNT-03)"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "src/components/nav/ClientSwitcher.tsx"
      provides: "Nav dropdown with status badges + search (D-01,D-05,D-08)"
  key_links:
    - from: "src/lib/clients.ts"
      to: "src/lib/db/schema.ts"
      via: "eq(socialAccounts.clientId, clientId)"
      pattern: "socialAccounts\\.clientId"
    - from: "src/components/nav/ClientSwitcher.tsx"
      to: "src/lib/clients.ts"
      via: "sets active_client_id cookie"
      pattern: "active_client_id"
    - from: "src/app/clients/[id]/connections/page.tsx"
      to: "src/lib/clients.ts"
      via: "getActiveClientId() to scope the connections query"
      pattern: "getActiveClientId"
---

<objective>
Build the isolated multi-client workspace vertical slice on top of the skeleton: client CRUD API with hard server-side scoping, the nav `ClientSwitcher` dropdown (active-client cookie, status badges, search), onboarding for zero-client users, and the default connections landing. This delivers CLNT-01 (create), CLNT-02 (isolation), CLNT-03 (manage separately) and honors nav decisions D-01…D-08.

Purpose: Client isolation is the highest-risk foundation concern (PITFALL 3) — a single missed scope = cross-client leak. This plan centralizes scoping in `requireClientScope()` + a NOT-NULL FK so every later phase inherits it.
Output: An authenticated user can create clients, switch the active client via a nav dropdown, and land on that client's connections list; all client-scoped reads are server-side scoped and cross-client reads return empty.
</objective>

<execution_context>
@/home/jhpy/Content-Creator/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jhpy/Content-Creator/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-CONTEXT.md (Decisions D-01…D-08)
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-SPEC.md (CLNT-01/02/03)
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-RESEARCH.md (Pattern 2: server-side scoping; PITFALL 3)
@/home/jhpy/Content-Creator/src/lib/db/schema.ts
@/home/jhpy/Content-Creator/src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 2-1: Client CRUD API + server-side scoping helper (CLNT-01, CLNT-03)</name>
  <read_first>
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-RESEARCH.md (Pattern 2, PITFALL 3)
    - /home/jhpy/Content-Creator/src/lib/db/schema.ts
    - /home/jhpy/Content-Creator/src/lib/auth.ts
  </read_first>
  <action>
    Create `src/lib/clients.ts` with the scoping chokepoint (PITFALL 3 mitigation):
    - `getActiveClientId(): Promise<string|null>` reads `active_client_id` cookie via `cookies()` from `next/headers` (server-side; D-02).
    - `requireClientScope()` — gets session via `auth.api.getSession`; if none → throw `401`; returns the session.userId. Used by all client routes.
    - `setActiveClientCookie(clientId)` writes the `active_client_id` httpOnly/Server-readable cookie (NOT a client-only cookie).
    - `listClients(userId)` selects from `client` where `userId = userId`.
    - `listConnections(clientId)` selects from `socialAccount` where `socialAccount.clientId = clientId` (hard server-side scoping; NEVER trust a clientId passed in the request body — D-02).
    - `resolveActiveClientId(): Promise<string|null>` (D-07 safe resolver — closes the stale-cookie bug where a deleted client's id remains in the `active_client_id` cookie and raw `getActiveClientId()` returns it, yielding an empty/broken view instead of auto-switching). Implementation: read `active_client_id` via `getActiveClientId()`; fetch the session's `userId` via `auth.api.getSession` (without throwing); if no session or no cookie value → return `null`. If a cookie value exists, verify it is present in `listClients(userId)`; if present return it, else fall back to the first client id from `listClients(userId)` (or `null` if the user has no clients — caller redirects to `/onboarding`). All call sites that render a scoped view MUST use `resolveActiveClientId()` instead of raw `getActiveClientId()`.

    Create `src/app/api/clients/route.ts`:
    - `GET`: require auth → return `listClients(userId)` (only the owner's clients).
    - `POST`: require auth → validate body with Zod (`{ name: string, min 1 }`) → insert `client` with `userId` → set `active_client_id` cookie to the new client → return 201 with the row. Unauthenticated → 401 (CLNT-01).

    Create `src/app/api/clients/[id]/route.ts`:
    - `GET` / `PATCH` / `DELETE`: require auth, then operate only where `client.id = :id AND client.userId = userId` (scope by owner, never by a body-supplied id).
    - `PATCH`: Zod-validate `{ name?: string }`; update only the matched row (PITFALL: PATCH X must not alter Y — enforced by the `userId` predicate).
    - `DELETE`: delete the matched client; the FK `onDelete: "cascade"` removes only that client's `socialAccount` + `oauthState` rows (CLNT-03 — delete removes only X's scoped resources).

    Write Wave-0 tests:
    - `src/app/api/clients/route.test.ts`: authed POST creates a client owned by the user (appears in GET list); unauthed POST → 401 (CLNT-01).
    - `src/app/api/clients/[id]/route.test.ts`: two clients list distinctly; PATCH one does not change the other; DELETE one removes only its own `socialAccount` rows (cascade) and not the other's (CLNT-03).
  </action>
  <verify>
    <automated>vitest run src/app/api/clients/route.test.ts src/app/api/clients/\[id\]/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Authed `POST /api/clients` creates a client owned by the user and sets the active-client cookie; unauthed → 401.
    - `GET /api/clients` returns only the authenticated user's clients.
    - `PATCH /api/clients/:id` updates only the owner-matched row (no cross-client change).
    - `DELETE /api/clients/:id` removes only that client's `socialAccount`/`oauthState` (cascade); other clients' data untouched.
    - Tests green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2-2: ClientSwitcher nav + onboarding + default connections landing + isolation test (CLNT-02, D-01…D-08)</name>
  <read_first>
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-CONTEXT.md (D-01…D-08)
    - /home/jhpy/Content-Creator/src/lib/clients.ts
    - /home/jhpy/Content-Creator/src/lib/db/schema.ts
  </read_first>
  <action>
    Create `src/components/nav/ClientSwitcher.tsx` (client component in the nav): fetches `/api/clients` to list clients; renders a dropdown (D-01) showing each client's `name` + status badges — connected-account count and a "Reconnect required" badge (D-05; count derived from a future connections endpoint — for now show 0 / placeholder, the badge wiring is finalized in Plan 03). Includes a client-side search/filter input (D-08). On select, POSTs to a small endpoint (or reuses `PATCH`/a dedicated `src/app/api/clients/active/route.ts`) that calls `setActiveClientCookie(clientId)` and refreshes. Auto-switch on active-client deletion (D-07) is handled by `resolveActiveClientId()` (defined in Task 2-1 / `src/lib/clients.ts`): it reads the cookie, validates it against `listClients(userId)`, and falls back to the first remaining client (or `null` → onboarding). The connections page and `AppNav` call `resolveActiveClientId()`, never raw `getActiveClientId()`, so a stale/deleted cookie id never produces a broken empty view. The ClientSwitcher's status badges are placeholders here (D-05 name + badge shell rendered) and are wired to REAL per-client connection counts in Plan 03 Task 3-3.

    Create `src/components/nav/AppNav.tsx` (server component) that reads `getActiveClientId()` and renders `ClientSwitcher` + nav links. Add `src/middleware.ts` (optional, lightweight): redirect unauthenticated users from protected routes to `/login` (use `auth.api.getSession` cookie check; avoid heavy DB in middleware — just presence check).

    Create `src/app/onboarding/page.tsx` (D-03): if the user has zero clients, after login they are redirected here (the dashboard/layout checks `listClients` length === 0 → redirect `/onboarding`); provides a create-client form calling `POST /api/clients`. Once a client exists, redirect to its connections landing.

    Create `src/app/clients/[id]/connections/page.tsx` (D-04 default landing): server component calls `resolveActiveClientId()` (NOT raw `getActiveClientId()` — D-07 safe); if it returns `null` → `redirect("/onboarding")`; otherwise requires auth + scope against the resolved client id, and renders the connections list (placeholder cards for now; full status cards + real badges in Plan 03). This is the default post-login screen once a client is selected.

    Write Wave-0 isolation test `src/lib/client-scope.test.ts` (DB-backed): insert a `socialAccount` row for client X (direct DB insert with valid `clientId`); assert `listConnections(clientY)` returns 0 rows (CLNT-02 — cross-client read empty). Assert inserting a `socialAccount` with a non-existent `clientId` throws a foreign-key error (CLNT-02 — FK prevents account without valid client). Assert `getActiveClientId()` returns the cookie value.
  </action>
  <verify>
    <automated>vitest run src/lib/client-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/client-scope.test.ts` green: cross-client `listConnections` returns empty; FK rejects invalid `clientId`; cookie read works.
    - `ClientSwitcher` renders clients from `/api/clients` with name + a search input; selecting sets the active-client cookie and refreshes.
    - Zero-client user is redirected to `/onboarding`; existing client → default landing is `/clients/[id]/connections`.
    - Active-client deletion auto-switches to the first remaining client (or onboarding) via `resolveActiveClientId()` — no broken/empty page; a stale deleted-client cookie id is validated against `listClients(userId)` and falls back (D-07).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/clients | Authenticated requests create/list/edit/delete clients |
| Server → PostgreSQL | Client-scoped queries filtered by owner + active client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation | `src/app/api/clients/route.ts` (CLNT-01) | mitigate | `requireClientScope()` (session check) on every route; unauthenticated → 401. Owner `userId` predicate never trusts a client-supplied id. |
| T-01-05 | Info Disclosure | `src/lib/clients.ts` scoping (CLNT-02) | mitigate | All reads filtered by `clientId` from the server cookie (`getActiveClientId`), never from request body (PITFALL 3). NOT-NULL FK on `socialAccount.clientId` enforces at DB. |
| T-01-06 | Tampering | `src/app/api/clients/[id]` (CLNT-03) | mitigate | `PATCH`/`DELETE` scoped by `client.id = :id AND client.userId = userId`; FK cascade removes only owned `socialAccount` rows. |
| T-01-07 | Spoofing | Client-supplied `clientId` in request body | mitigate | `clientId` is sourced ONLY from the `active_client_id` cookie / owner predicate; request-body `clientId` is ignored for scoping (D-02). |
</threat_model>

<verification>
- Client API tests green (create/unauth-401, list distinct, PATCH isolation, DELETE cascade).
- `src/lib/client-scope.test.ts` green (cross-client empty, FK enforced).
- Manual: `npm run dev` → sign up → onboarding (zero clients) → create client → nav dropdown shows it → select → lands on connections page → refresh keeps active client.
</verification>

<success_criteria>
- [ ] Authenticated user can create a client; unauthenticated → 401 (CLNT-01).
- [ ] Request scoped to client Y returns no rows for client X's accounts; FK rejects orphan accounts (CLNT-02).
- [ ] Two clients managed separately; delete cascades only that client's resources (CLNT-03).
- [ ] Nav dropdown selects active client via server-readable cookie; onboarding + default connections landing work (D-01…D-08).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-clients-connections/01-02-SUMMARY.md` when done.
</output>
