---
phase: "01"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - tailwind source css (src/app/globals.css)
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/login/page.tsx
  - src/app/dashboard/page.tsx
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/lib/db.ts
  - src/lib/db/schema.ts
  - drizzle.config.ts
  - vitest.config.ts
  - vitest.setup.ts
  - .env.example
  - src/app/api/auth/[...all]/route.ts
autonomous: false
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "A team member can sign up with email/password and a user row is created with a non-plaintext password hash"
    - "A team member can log in and a secure session cookie is issued"
    - "After login, a full page refresh leaves the user authenticated (session cookie still valid)"
    - "The dashboard server component reads the session and shows the logged-in user's email"
    - "All 5 tables (user, session, client, social_account, oauth_state) exist in Postgres after drizzle-kit push"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Drizzle schema: user, session, client, socialAccount, oauthState"
      contains: "export const user = pgTable|export const client = pgTable|export const socialAccount = pgTable|export const oauthState = pgTable"
    - path: "src/lib/auth.ts"
      provides: "Better Auth instance (Drizzle adapter, emailAndPassword enabled, session config)"
      exports: ["auth"]
    - path: "src/app/dashboard/page.tsx"
      provides: "Server component that calls auth.api.getSession and renders user email"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.signIn.email() / signUp.email()"
      pattern: "authClient\\.sign(In|Up)\\.email"
    - from: "src/lib/auth.ts"
      to: "src/lib/db.ts"
      via: "drizzleAdapter(db, { provider: 'pg' })"
      pattern: "drizzleAdapter\\(db"
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/auth.ts"
      via: "auth.api.getSession({ headers })"
      pattern: "auth\\.api\\.getSession"
---

<objective>
Scaffold the entire Content-Creator foundation (Next.js 15.5 App Router + Drizzle + Better Auth) and prove the full stack with a working team-auth vertical slice, then harden it with the exact SPEC auth paths and tests. This is the Walking Skeleton (per SKELETON.md): one real DB write (signup), one real DB read (session), and one real UI interaction (login form) wired end-to-end.

Purpose: Establish the architectural substrate every later phase depends on — framework, DB client, auth, the full Drizzle schema (all 5 tables), and the dev run command — and validate AUTH-01 (signup/login) + AUTH-02 (session survives refresh) against it.
Output: A runnable Next.js app where a user can sign up, log in, and see their email on a dashboard that survives refresh; all tables created in Postgres.
</objective>

<execution_context>
@/home/jhpy/Content-Creator/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jhpy/Content-Creator/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jhpy/Content-Creator/AGENTS.md
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-SPEC.md
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-RESEARCH.md
@/home/jhpy/Content-Creator/.planning/research/STACK.md
@/home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/SKELETON.md
# Better Auth Drizzle adapter field names (source of truth for schema.ts):
#   better-auth.com/docs/adapters/drizzle  (pg provider; user/session/account/verification tables)
</context>

<tasks>

<task type="auto">
  <name>Task 1-1: Scaffold Next.js 15 + Drizzle + Better Auth + full schema + skeleton UI</name>
  <read_first>
    - /home/jhpy/Content-Creator/AGENTS.md
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-RESEARCH.md (§Standard Stack, §Architecture Patterns)
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/SKELETON.md
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-SPEC.md (Goal, Constraints)
  </read_first>
  <action>
    Initialize the greenfield app. Install (do NOT run `npm i next@latest`): `next@^15.5.19 react@^19 react-dom@^19`, `better-auth@1.6.23 @better-auth/drizzle-adapter@1.6.23`, `drizzle-orm@^0.45.2 postgres@^3.4`, `zod@^4`; dev: `typescript@^5.7 @types/node @types/react @types/react-dom drizzle-kit@^0.31 tailwindcss@^4 @tailwindcss/postcss vitest`. Configure `tsconfig.json` (paths `@/*` → `src/*`, `moduleResolution: bundler`), `next.config.ts`, Tailwind v4 via `@tailwindcss/postcss` in `postcss.config.mjs` + `src/app/globals.css` with `@import "tailwindcss";`.

    Create `src/lib/db.ts`: a Drizzle client using `postgres` driver from `DATABASE_URL` (`import postgres from "postgres"; const sql = postgres(process.env.DATABASE_URL!); export const db = drizzle(sql, { schema: ... })`).

    Create `src/lib/db/schema.ts` with ALL 5 tables (this is the DB contract for the whole phase; plans 02/03 implement logic against these, no further schema changes):
    - Better Auth tables following the official Drizzle adapter field names for `provider: "pg"`: `user` (id, email, name, hashed password column named per adapter docs, emailVerified, createdAt, updatedAt), `session` (id, userId FK→user, token, expiresAt, ipAddress, userAgent, createdAt, updatedAt), `account` (id, userId FK, accountId, providerId, accessToken, refreshToken, idToken, accessTokenExpiresAt, refreshTokenExpiresAt, scope, password, createdAt, updatedAt), `verification` (id, identifier, value, expiresAt, createdAt, updatedAt).
    - `client` (id uuid pk default gen_random_uuid(), userId text NOT NULL FK→user.id onDelete: "cascade", name text NOT NULL, createdAt, updatedAt).
    - `socialAccount` (id uuid pk, clientId uuid NOT NULL FK→client.id onDelete: "cascade", platform text NOT NULL check in ('meta','linkedin'), platformAccountId text NOT NULL, name text, accessTokenEnc text NOT NULL, refreshTokenEnc text, iv text NOT NULL, tag text NOT NULL, expiresAt timestamp, keyVersion int NOT NULL default 1, createdAt, updatedAt).
    - `oauthState` (id uuid pk, provider text NOT NULL, state text NOT NULL unique, codeVerifier text NOT NULL, clientId uuid NOT NULL FK→client.id onDelete: "cascade", redirectUri text NOT NULL, expiresAt timestamp NOT NULL, createdAt).

    Create `src/lib/auth.ts`: `betterAuth({ database: drizzleAdapter(db, { provider: "pg" }), emailAndPassword: { enabled: true, minPasswordLength: 8 }, session: { expiresIn: 60*60*24*7, updateAge: 60*60*24 }, baseURL: process.env.BETTER_AUTH_URL, secret: process.env.BETTER_AUTH_SECRET })`. Create `src/lib/auth-client.ts`: `createAuthClient()` with `emailAndPassword: {}` (better-auth/react). Mount catch-all `src/app/api/auth/[...all]/route.ts` exporting `toNextJsHandler(auth)` (GET, POST) — this provides `/api/auth/sign-up/email` and `/api/auth/sign-in/email`.

    Skeleton UI (proves full stack): `src/app/layout.tsx` (html/body + globals.css), `src/app/page.tsx` server component → `redirect("/dashboard")` (or `/login` if no session helper). `src/app/login/page.tsx` ("use client") with a form that calls `authClient.signUp.email({ email, password, name })` then `authClient.signIn.email({ email, password })` and on success `router.push("/dashboard")` — this is the real UI interaction wired to the API. `src/app/dashboard/page.tsx` server component: `const session = await auth.api.getSession({ headers: await headers() }); if (!session) redirect("/login");` then render `session.user.email` (real DB session read). This proves AUTH-02 (cookie survives the server component call).

    Create `drizzle.config.ts` pointing at `src/lib/db/schema.ts` + `DATABASE_URL`. Create `vitest.config.ts` (`test.environment: "node"`, `test.setupFiles: ["vitest.setup.ts"]`, include `src/**/*.test.ts`) and `vitest.setup.ts` loading `DATABASE_URL_TEST` (fallback to `DATABASE_URL`). Create `.env.example` documenting `DATABASE_URL`, `DATABASE_URL_TEST`, `BETTER_AUTH_SECRET` (32+ bytes), `BETTER_AUTH_URL`, `TOKEN_ENCRYPTION_KEY` (32-byte base64, generate via `openssl rand -base64 32`), `OAUTH_PROVIDER_MODE=mock`, `META_CLIENT_ID`, `META_CLIENT_SECRET`, `LINKEDIN_CLIENT_ID`, `LINKEDIN_CLIENT_SECRET`. Write these (with generated secrets) into `.env.local` so the app runs.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds (Next 15.5 compiles; no TS errors).
    - `src/lib/db/schema.ts` exports `user`, `session`, `client`, `socialAccount`, `oauthState` tables.
    - `src/app/login/page.tsx` calls `authClient.signUp.email` / `authClient.signIn.email` (grep confirms).
    - `src/app/dashboard/page.tsx` calls `auth.api.getSession` and renders `session.user.email`.
    - `.env.local` exists with `DATABASE_URL` and a generated `TOKEN_ENCRYPTION_KEY` (base64 length 44) and `BETTER_AUTH_SECRET`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1-2: [BLOCKING] drizzle-kit push — create all tables</name>
  <what-built>The agent ran `npx drizzle-kit push` against `DATABASE_URL` to create user, session, account, verification, client, social_account, oauth_state tables. The agent reports the push output.</what-built>
  <how-to-verify>
    1. Confirm the agent's `npx drizzle-kit push` output shows all tables created (or "no changes").
    2. Verify against the DB: run `SELECT table_name FROM information_schema.tables WHERE table_schema='public' ORDER BY table_name;` — expected rows: account, client, oauth_state, session, social_account, user, verification (names may differ if adapter uses pluralization — confirm the Better Auth + our 3 custom tables exist).
    3. If `drizzle-kit push` prompted for confirmation on new-table creation, the agent confirmed it (safe, non-destructive on a fresh DB).
  </how-to-verify>
  <resume-signal>Type "approved" once tables exist, or paste the push error to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 1-3: SPEC-exact auth wrappers + AUTH-01/02 tests</name>
  <read_first>
    - /home/jhpy/Content-Creator/.planning/phases/01-foundation-auth-clients-connections/01-SPEC.md (AUTH-01, AUTH-02 acceptance)
    - /home/jhpy/Content-Creator/src/lib/auth.ts
    - /home/jhpy/Content-Creator/src/app/api/auth/[...all]/route.ts
  </read_first>
  <action>
    Add thin SPEC-exact route handlers (the catch-all already handles Better Auth; these add the exact paths from SPEC):
    - `src/app/api/auth/signup/route.ts`: POST → `auth.api.signUpEmail({ body, headers: req.headers, asResponse: true })`; return its JSON + status + headers.
    - `src/app/api/auth/login/route.ts`: POST → `auth.api.signInEmail({ body, headers: req.headers, asResponse: true })`.
    These satisfy SPEC's literal `POST /api/auth/signup` and `POST /api/auth/login` while Better Auth keeps `/api/auth/sign-up/email` etc. (RESEARCH A6).

    Write Wave-0 tests (Vitest, `environment: node`):
    - `src/app/api/auth/signup/route.test.ts`: POST valid email+password → 200 and a `user` row exists with `password` (or the adapter's hashed column) NOT equal to the input; duplicate email → 409 (or 400 with duplicate error); stored password ≠ plaintext input.
    - `src/app/api/auth/login/route.test.ts`: correct creds → 200 + Set-Cookie session; wrong password → 401 (no session cookie); unknown email → 401.
    - `src/lib/auth-session.test.ts`: after login, a second request reusing the session cookie returns the authenticated session (AUTH-02 — survives refresh); a request with no/invalid cookie returns null session / 401.
    Use a test DB via `DATABASE_URL_TEST` (Neon/Supabase free tier, or PGlite). Assert password non-plaintext by selecting the hashed column and asserting `!==` the input.
  </action>
  <verify>
    <automated>vitest run src/app/api/auth/signup/route.test.ts src/app/api/auth/login/route.test.ts src/lib/auth-session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run` for the 3 auth test files is green.
    - Signup with unique email creates a user with a non-plaintext password hash; duplicate email rejected (409/duplicate error).
    - Correct credentials return a session cookie; wrong/unknown → 401 and no session cookie.
    - A request replaying the session cookie is authenticated (refresh survives); no/invalid cookie → unauthenticated.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js API | Login/signup form posts credentials; session cookie replayed |
| Next.js → PostgreSQL | Auth + user rows written/read via Drizzle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `src/app/api/auth/*` (AUTH-01) | mitigate | Better Auth `emailAndPassword` — scrypt password hashing; duplicate-email rejection (409); no plaintext password stored. |
| T-01-02 | Spoofing | Session cookie (AUTH-02) | mitigate | Better Auth DB-backed session cookie: `HttpOnly`, `SameSite=Lax`, `Secure` in prod; `expiresIn` 7d; validated on every server-component request via `auth.api.getSession`. |
| T-01-03 | Info Disclosure | `.env` / secrets | mitigate | `BETTER_AUTH_SECRET` + `TOKEN_ENCRYPTION_KEY` server-only env; `.env.local` git-ignored; never logged; template in `.env.example`. |
| T-01-SC | Tampering | npm installs | mitigate | Packages pre-verified on npm registry (01-RESEARCH §Package Legitimacy Audit); pin exact versions; do NOT run `npm i next@latest`. |
</threat_model>

<verification>
- `npm run build` passes (skeleton compiles).
- `npx drizzle-kit push` creates all 7 tables (human-verified checkpoint).
- Auth test trio green: signup hashed+duplicate-rejected, login 401 on bad creds, session survives refresh.
- Manual: `npm run dev`, open `/login`, sign up, land on `/dashboard` showing email, hard-refresh → still authenticated.
</verification>

<success_criteria>
- [ ] Team member can sign up (hashed password, duplicate rejected) — AUTH-01.
- [ ] Team member can log in (session cookie issued, 401 on bad creds) — AUTH-01.
- [ ] Session survives browser refresh (dashboard re-renders from cookie) — AUTH-02.
- [ ] Full DB schema (user, session, client, social_account, oauth_state + Better Auth account/verification) exists in Postgres.
- [ ] `npm run dev` serves a working full-stack skeleton.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-clients-connections/01-01-SUMMARY.md` when done.
</output>
