---
phase: "06"
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - src/lib/publish/index.ts
  - src/app/api/posts/[id]/publish/route.ts
  - src/components/compose/PublishModal.tsx
  - src/app/compose/new/page.tsx
files_created:
  - src/lib/publish/linkedin.ts
  - src/lib/publish/linkedin.test.ts
autonomous: true
requirements: ["PUBL-01", "PUBL-02", "PUBL-03"]
must_haves:
  truths:
    - "Team can publish a text post to a connected LinkedIn account via the adapter pipeline — same PublishModal, same BullMQ worker, same per-target status tracking."
    - "Team can publish a post with a single image (JPEG/PNG/GIF) to LinkedIn via the adapter pipeline."
    - "LinkedInPublisher implements the Publisher interface (prepare + publish + verify), using LinkedIn REST API endpoints with Bearer auth and LinkedIn-Version header."
    - "LinkedIn accounts in PublishModal show a reconnect-required badge when token is within 7 days of expiry; expired tokens are grayed/disabled in the selector."
    - "LinkedIn-specific limits (700 char caption, single image max 5MB, JPEG/PNG/GIF only) are validated in prepare() and surfaced in the composer."
    - "LinkedIn organic carousels are explicitly documented as unsupported — the composer shows a notice when the user adds 2+ media items and selects LinkedIn."
    - "Publish status per LinkedIn target is tracked (scheduled/running/published/failed) with raw LinkedIn errors surfaced in PublishStatusView."
  artifacts:
    - path: "src/lib/publish/linkedin.ts"
      provides: "LinkedInPublisher — implements Publisher for LinkedIn REST API (prepare + publish + verify)."
    - path: "src/lib/publish/linkedin.test.ts"
      provides: "Unit tests for LinkedInPublisher with mocked fetch."
  key_links:
    - from: "src/lib/publish/linkedin.ts"
      to: "src/lib/publish/provider.ts"
      via: "implements Publisher interface"
    - from: "src/lib/publish/index.ts"
      to: "src/lib/publish/linkedin.ts"
      via: "factory returns LinkedInPublisher for 'linkedin' platform"
    - from: "worker.ts"
      to: "src/lib/publish/index.ts"
      via: "getPublisher('linkedin') — unchanged, already generic"
    - from: "src/components/compose/PublishModal.tsx"
      to: "src/lib/connection-status.ts"
      via: "statusFor() for reconnect badge on LinkedIn accounts"
    - from: "src/app/api/posts/[id]/publish/route.ts"
      to: "src/lib/db/schema.ts"
      via: "inArray filter includes 'linkedin' accounts"
---

<objective>
Implement LinkedIn publisher (Phase 6): extend the per-platform `Publisher` adapter to LinkedIn, enabling immediate publish of text and text+single-image posts to connected LinkedIn accounts. Surface the 60-day token expiry with reconnect badges in PublishModal and disable expired tokens gracefully. Add LinkedIn-specific validation and carousel-unsupported messaging in the composer. All publish flows reuse the existing BullMQ worker pipeline and per-target status tracking established in Phase 3.

Purpose: Deliver the third and final per-platform publish adapter (LinkedIn), completing multi-platform coverage for PUBL-01/02/03.
Output: LinkedInPublisher adapter, updated factory, updated publish route, updated PublishModal with LinkedIn tab + reconnect state, composer warnings, and tests.
</objective>

<execution_context>
@/home/jhpy/Content-Creator/.opencode/get-shit-done/workflows/execute-plan.md
@/home/jhpy/Content-Creator/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jhpy/Content-Creator/.planning/phases/06-publish-to-linkedin/06-CONTEXT.md (Decisions D-01…D-14)
@/home/jhpy/Content-Creator/.planning/phases/05-publish-to-instagram/05-01-SUMMARY.md (InstagramPublisher pattern reference)
@/home/jhpy/Content-Creator/src/lib/publish/provider.ts (Publisher interface — already includes "linkedin" platform type)
@/home/jhpy/Content-Creator/src/lib/publish/meta.ts (MetaPublisher — reference implementation to mirror for LinkedInPublisher)
@/home/jhpy/Content-Creator/src/lib/publish/index.ts (factory — must add LinkedInPublisher before FakePublisher fallback)
@/home/jhpy/Content-Creator/src/lib/publish/fake.ts (FakePublisher — reference, already handles "linkedin" when mode is "fake")
@/home/jhpy/Content-Creator/src/lib/oauth/linkedin.ts (LinkedInOAuthProvider — fully implemented, PKCE, w_member_social scope)
@/home/jhpy/Content-Creator/src/lib/connection-status.ts (statusFor — already flags 60-day expiry with configurable 7-day threshold)
@/home/jhpy/Content-Creator/src/lib/crypto.ts (decrypt for token at publish time)
@/home/jhpy/Content-Creator/src/lib/db/schema.ts (social_account platform already includes 'linkedin'; publish_targets exists)
@/home/jhpy/Content-Creator/src/app/api/posts/[id]/publish/route.ts (current route — accepts "meta" and "instagram", must add "linkedin")
@/home/jhpy/Content-Creator/src/components/compose/PublishModal.tsx (current — Facebook + Instagram tabs, must add LinkedIn tab with reconnect state)
@/home/jhpy/Content-Creator/src/app/compose/new/page.tsx (composer — current warnings for IG/LinkedIn, must add carousel unsupported notice)
@/home/jhpy/Content-Creator/worker.ts (unchanged — already generic, processes any platform via getPublisher)
@/home/jhpy/Content-Creator/.planning/ROADMAP.md (Phase 6 goal, success criteria)
@/home/jhpy/Content-Creator/.planning/REQUIREMENTS.md (PUBL-01/02/03 — LinkedIn coverage)
</context>

<interfaces>
<![CDATA[
From src/lib/publish/provider.ts:
  export type PublishPlatform = "meta" | "linkedin" | "instagram" | "fake";
  export interface Publisher {
    platform: PublishPlatform;
    prepare(post: unknown, target: unknown): Promise<PrepareResult>;
    publish(post: unknown, target: unknown, context: PublishContext): Promise<PublishResult>;
    verify(targetId: string, platformRef: string): Promise<VerifyResult>;
  }

From src/lib/connection-status.ts:
  export type ConnectionStatus = "connected" | "reconnect_required";
  export function statusFor(expiresAt: Date | null | undefined): ConnectionStatus;

From src/lib/db/schema.ts:
  socialAccount has columns: id (uuid), clientId, platform (text), platformAccountId (text), name (text|null),
                            accessTokenEnc (text), iv (text), tag (text), expiresAt (timestamp), keyVersion (int)
  publishTargets has columns: id (uuid), postId, socialAccountId, status (text), errorMessage (text|null),
                              publishedAt (timestamp), createdAt, updatedAt

From src/lib/publish/meta.ts (reference pattern):
  prepare() validates content limits, returns { ready: boolean, errors?: string[] }
  publish() uploads media via public URL, posts to platform API, returns { success, platformRef, error? }
  verify() returns { status: "published", platformRef }

From src/components/compose/PublishModal.tsx:
  Props: postId, facebookAccounts, instagramAccounts, isOpen, onClose, onPublished
  Has activeTab state: "facebook" | "instagram"
]]>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LinkedInPublisher implementation</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/lib/publish/provider.ts (Publisher interface — contracts to implement)
    - /home/jhpy/Content-Creator/src/lib/publish/meta.ts (MetaPublisher — pattern to mirror for structure, error handling)
    - /home/jhpy/Content-Creator/src/lib/oauth/linkedin.ts (LinkedIn API constants: LINKEDIN_API = "https://api.linkedin.com/rest", LinkedIn-Version: "202405", Bearer auth pattern)
    - /home/jhpy/Content-Creator/.planning/phases/06-publish-to-linkedin/06-CONTEXT.md (D-01 through D-07, D-12, D-14)
  </read_first>
  <action>
    Create `src/lib/publish/linkedin.ts` — `LinkedInPublisher` implementing `Publisher`:

    - Import `Publisher`, `PublishPlatform`, `PrepareResult`, `PublishResult`, `VerifyResult`, `PublishContext` from `./provider`.
    - Use `LINKEDIN_API = "https://api.linkedin.com/rest"` constant imported from or matching `src/lib/oauth/linkedin.ts`.
    - Use `LINKEDIN_HEADERS` object: `{ Authorization: "Bearer ${accessToken}", "LinkedIn-Version": "202405", "X-Restli-Protocol-Version": "2.0.0", "Content-Type": "application/json" }`. Note: the `Authorization` header is set at call time with the bearer token (not a module-level constant).

    Class: `LinkedInPublisher`
    - `platform: PublishPlatform = "linkedin"`.

    `prepare(post, target) → PrepareResult`:
    - If no text and no media → error: "Post must have text or media for LinkedIn".
    - If text > 700 characters → error: "LinkedIn caption exceeds 700 character limit" (D-05, standard 700-char limit for LinkedIn posts).
    - If media length > 1 → error: "LinkedIn supports a single image only" (D-04, D-05 — no carousel, no multi-image).
    - If media.length === 1, validate single image:
      - contentType must be image/jpeg, image/png, or image/gif. If not → error: "LinkedIn image must be JPEG, PNG, or GIF" (D-02).
      - Note: max 5MB file size cannot be validated without the actual file; skip size validation in prepare() (D-02 notes it, but we don't have the binary — accept as agent's discretion to skip programmatic size check since we only have content-type and URL).
    - If media has video contentType → error: "LinkedIn video publishing not supported in this phase" (D-04).
    - If post has 2+ media items → error: "LinkedIn does not support carousel posts" (D-07).
    - Return `{ ready: errors.length === 0, errors: errors.length ? errors : undefined }`.

    `publish(post, target, context) → PublishResult`:
    - Extract `accessToken` from `context`.
    - Extract `linkedinUserId` from `target.socialAccount?.platformAccountId` or `context.linkedinUserId`. If missing → return `{ success: false, error: "No LinkedIn user ID in target" }`.
    - Extract `mediaItems` from `post.media || []`.
    - Extract `text` from `post.text || ""`.
    - `authorUrn = "urn:li:person:" + linkedinUserId`.
    - `mediaRef` variable (string | undefined) for tracking uploaded image URN.

    If mediaItems.length === 1 (single image):
    - Use `POST ${LINKEDIN_API}/images?action=initializeUpload` with headers (Authorization, LinkedIn-Version, X-Restli-Protocol-Version, Content-Type).
    - Body: `{ "initializeUploadRequest": { "owner": authorUrn } }`.
    - Parse response JSON: `{ value: { uploadUrl: string, image: string } }`. The `image` field is the URN like `urn:li:image:xyz`.
    - Perform a PUT to `uploadUrl` with the image binary. Use `fetch(uploadUrl, { method: "PUT", body: blob })` where blob = construct from the media's publicUrl. Fetch the image from `m.publicUrl` first via GET and pass the response body to the PUT.
    - On success: `mediaRef = uploadResponse.image` (the URN).
    - On any failure in the upload flow: set `mediaFailed = true` (D-03).

    Create the post via `POST ${LINKEDIN_API}/posts`:
    - If text-only (no media):
      - Body: `{ "author": authorUrn, "commentary": text, "visibility": "PUBLIC", "lifecycleState": "PUBLISHED", "distribution": { "feedDistribution": "MAIN_FEED", "targetEntities": [], "thirdPartyDistributionChannels": [] } }`.
    - If text + image (mediaRef set):
      - Body: same as above + `"content": { "media": { "id": mediaRef } }` (D-04, D-05 — single image).
    - If media upload failed before (mediaFailed = true but text has content):
      - Post text-only with `error` field in return = `"published (media failed)"` (D-03).

    Parse response. Success: status 201 with `{ id: "urn:li:share:xyz" }`. 
    - Return `{ success: true, platformRef: response.id }`.
    - Return `{ success: false, error: "LinkedIn API error: " + (response.status + " " + JSON.stringify(rawBody)) }` on failure.
    - On error with JSON body containing `serviceErrorCode` — include it in the error string per D-12.

    `verify(_targetId, platformRef) → VerifyResult`:
    - Return `{ status: "published", platformRef }`. LinkedIn post IDs are final on creation.

    No changes to worker.ts — it already calls `getPublisher("linkedin")` generically.
  </action>
  <acceptance_criteria>
    - LinkedInPublisher implements Publisher interface (prepare, publish, verify) exactly
    - prepare() validates text ≤ 700 chars (D-05)
    - prepare() validates single image only (no carousel, no video) (D-04, D-05)
    - prepare() validates image format is JPEG/PNG/GIF only (D-02)
    - prepare() returns "LinkedIn does not support carousel posts" for 2+ media items (D-07)
    - publish() uploads image via POST /rest/images?action=initializeUpload + PUT to upload URL
    - publish() creates post via POST /rest/posts with author URN, commentary, visibility PUBLIC
    - publish() attaches image via content.media.id when image upload succeeds
    - publish() falls back to text-only with "published (media failed)" when image upload fails (D-03)
    - publish() uses Authorization Bearer + LinkedIn-Version: 202405 headers
    - publish() includes raw serviceErrorCode in error string on API failure (D-12)
    - verify() returns published status with platformRef
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/publish/linkedin.ts 2>&1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update publisher factory for LinkedIn</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/lib/publish/index.ts (current factory — must add LinkedInPublisher before FakePublisher)
    - /home/jhpy/Content-Creator/src/lib/publish/linkedin.ts (new LinkedInPublisher)
  </read_first>
  <action>
    Update `src/lib/publish/index.ts`:

    1. Add import: `import { LinkedInPublisher } from "./linkedin";`
    2. In the `getPublisher()` function, inside the `if (mode === "real")` block, add before the `return new FakePublisher(platform)` fallback:
       ```
       if (platform === "linkedin") return new LinkedInPublisher();
       ```

    The function should now read:
    ```typescript
    export function getPublisher(platform?: PublishPlatform): Publisher {
      const mode = process.env.PUBLISHER_MODE ?? "fake";
      if (mode === "real") {
        if (platform === "meta") return new MetaPublisher();
        if (platform === "instagram") return new InstagramPublisher();
        if (platform === "linkedin") return new LinkedInPublisher();
        return new FakePublisher(platform);
      }
      return new FakePublisher(platform ?? "fake");
    }
    ```

    No other changes needed — FakePublisher fallback already handles `"linkedin"` when mode is `"fake"`.
  </action>
  <acceptance_criteria>
    - getPublisher("linkedin") with PUBLISHER_MODE=real returns LinkedInPublisher
    - getPublisher("linkedin") with PUBLISHER_MODE=fake returns FakePublisher (already worked)
    - getPublisher() without platform defaults to FakePublisher (unchanged)
    - Factory ordering: meta → instagram → linkedin → FakePublisher fallback
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --strict src/lib/publish/index.ts 2>&1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update publish API endpoint for LinkedIn accounts</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/app/api/posts/[id]/publish/route.ts (current route — accepts "meta" and "instagram")
    - /home/jhpy/Content-Creator/src/lib/db/schema.ts (socialAccount.platform values: 'meta', 'linkedin', 'instagram')
  </read_first>
  <action>
    Update `src/app/api/posts/[id]/publish/route.ts`:

    1. Change the `inArray` filter on line 48 from `["meta", "instagram"]` to `["meta", "instagram", "linkedin"]`:
       ```typescript
       inArray(socialAccount.platform, ["meta", "instagram", "linkedin"]),
       ```

    This is the only change needed. The existing logic:
    - Already uses `account?.platform` when enqueuing the BullMQ job (line ~78: `platform: account?.platform ?? "meta"`), so LinkedIn-targeted jobs will correctly set `platform: "linkedin"`.
    - Already validates account existence and client scoping.
    - Already handles the per-target status tracking.

    No other route changes needed.
  </action>
  <acceptance_criteria>
    - POST /api/posts/[id]/publish accepts socialAccount rows with platform "linkedin"
    - Enqueues BullMQ jobs with platform: "linkedin" for LinkedIn accounts
    - Existing Meta and Instagram account validation still works (backward compatible)
    - Returns 201 with target IDs for status polling (same as Meta/IG)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --strict src/app/api/posts/\[id\]/publish/route.ts 2>&1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Update PublishModal with LinkedIn tab + reconnect status</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/components/compose/PublishModal.tsx (existing — Facebook + Instagram tabs, SocialAccount type)
    - /home/jhpy/Content-Creator/src/lib/connection-status.ts (statusFor() — returns "connected" | "reconnect_required")
    - /home/jhpy/Content-Creator/.planning/phases/06-publish-to-linkedin/06-CONTEXT.md (D-08, D-09, D-10, D-11)
  </read_first>
  <action>
    Update `src/components/compose/PublishModal.tsx`:

    1. Add `linkedinAccounts: SocialAccount[]` to the `PublishModalProps` interface.
    2. Add `"linkedin"` to the `Tab` type: `type Tab = "facebook" | "instagram" | "linkedin";`
    3. Destructure `linkedinAccounts` from props alongside existing accounts.
    4. Add LinkedIn tab button in the tab bar (after Instagram):
       ```tsx
       <button
         onClick={() => setActiveTab("linkedin")}
         className={`px-3 py-2 text-sm font-medium border-b-2 -mb-px ${
           activeTab === "linkedin" ? "border-blue-500 text-blue-600" : "border-transparent text-gray-500 hover:text-gray-700"
         }`}
       >
         LinkedIn ({linkedinAccounts.length})
       </button>
       ```
    5. Import `statusFor` from `@/lib/connection-status` (add import line).
    6. Update the `currentAccounts` mapping: for the `"linkedin"` tab, use `linkedinAccounts`. For the LinkedIn accounts, render each account row with:
       - A red badge "Reconnect required" next to the account name when `statusFor(acc.expiresAt) === "reconnect_required"` (D-08).
       - The checkbox `disabled={statusFor(acc.expiresAt) === "reconnect_required"}` — expired/near-expiry LinkedIn tokens are grayed out (D-11).
       - For disabled/reconnect-required items, add a clickable "Reconnect" link pointing to `/clients` (the connections page) (D-10).
       - Show a non-disruptive tooltip text: "Token expires soon — reconnect to continue publishing" when reconnect_required.
    7. Update the `hasAnyAccounts` check to also include linkedinAccounts.
    8. Update the empty state text: update the per-tab empty state to show the correct account type name.
    9. All other behavior (handlePublish, toggleAccount, selectedIds) remains unchanged — the same set tracks selections across all three tabs.

    The render logic for each LinkedIn account row should look like:
    ```tsx
    {currentAccounts.map((acc) => {
      const connStatus = statusFor(acc.expiresAt);
      const isLinkedInReconnect = connStatus === "reconnect_required";
      return (
        <label
          key={acc.id}
          className={`flex items-center gap-3 p-3 rounded border cursor-pointer ${
            selectedIds.has(acc.id) ? "border-blue-500 bg-blue-50" :
            isLinkedInReconnect ? "border-red-200 bg-red-50 opacity-60" : "border-gray-200 hover:bg-gray-50"
          }`}
        >
          <input
            type="checkbox"
            checked={selectedIds.has(acc.id)}
            onChange={() => toggleAccount(acc.id)}
            disabled={isLinkedInReconnect}
            className="h-4 w-4 text-blue-600"
          />
          <div className="flex-1">
            <div className="font-medium text-sm flex items-center gap-2">
              {acc.name}
              {isLinkedInReconnect && (
                <span className="inline-block text-xs bg-red-100 text-red-700 px-2 py-0.5 rounded font-medium">
                  Reconnect required
                </span>
              )}
            </div>
            <div className="text-xs text-gray-500">{acc.platformAccountId}</div>
            {isLinkedInReconnect && (
              <a href="/clients" className="text-xs text-blue-600 underline mt-1 inline-block">
                Reconnect now →
              </a>
            )}
          </div>
        </label>
      );
    })}
    ```
  </action>
  <acceptance_criteria>
    - PublishModal shows LinkedIn tab with count badge (D-08)
    - LinkedIn tab lists connected LinkedIn accounts
    - Reconnect-required accounts (within 7-day threshold per connection-status.ts) show red badge "Reconnect required" (D-08)
    - Reconnect-required accounts are grayed/disabled in the selector (D-11)
    - Disabled accounts show "Reconnect now →" link to /clients page (D-10)
    - Non-expired LinkedIn accounts are selectable normally
    - All three tabs work correctly (Facebook, Instagram, LinkedIn)
    - Selection is shared across tabs
    - Other account platforms (FB, IG) are unaffected when a LinkedIn account is expired
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --strict src/components/compose/PublishModal.tsx 2>&1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 5: Update compose page with LinkedIn carousel notice + limited account fetch</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/app/compose/new/page.tsx (current — LinkedIn warning at 700 chars already present, fetches FB + IG accounts)
    - /home/jhpy/Content-Creator/.planning/phases/06-publish-to-linkedin/06-CONTEXT.md (D-07: carousel explicit unsupported message)
  </read_first>
  <action>
    Update `src/app/compose/new/page.tsx`:

    1. Add LinkedIn accounts fetching alongside existing FB/IG fetches:
       ```typescript
       const [linkedinAccounts, setLinkedinAccounts] = useState<any[]>([]);

       // In useEffect alongside the existing fetches:
       fetch("/api/social-accounts?platform=linkedin")
         .then((r) => r.json())
         .then((data) => setLinkedinAccounts(data))
         .catch(() => {});
       ```

    2. Add a LinkedIn carousel unsupported notice (D-07):
       - Detect when the user has multiple media items. Add a state `mediaCount` (or similar — the page may have media upload logic). If there's no existing multi-image state, add a simple `mediaCount` state tracked by upload events.
       - Render a notice when `mediaCount >= 2`: 
         ```tsx
         <div className="bg-yellow-100 border border-yellow-400 text-yellow-800 px-3 py-2 rounded text-sm">
           <strong>LinkedIn:</strong> Carousel posts are not supported on LinkedIn.
           Facebook and Instagram support carousels.
         </div>
         ```
       - Position this after the existing warnings block.

    3. Update the "Publish Now" button disabled condition — currently disabled when both `facebookAccounts.length === 0 && instagramAccounts.length === 0`. Change to `facebookAccounts.length === 0 && instagramAccounts.length === 0 && linkedinAccounts.length === 0`.

    4. Update the tooltip text: change "No connected accounts" to "No connected accounts" (already works as a general message).

    5. Pass `linkedinAccounts` to the `PublishModal` component:
       ```tsx
       <PublishModal
         postId={postId}
         facebookAccounts={facebookAccounts}
         instagramAccounts={instagramAccounts}
         linkedinAccounts={linkedinAccounts}
         isOpen={showPublishModal}
         onClose={() => setShowPublishModal(false)}
         onPublished={handlePublished}
       />
       ```

    6. The existing LinkedIn 700-char warning is already present (line 40: `...text.length > 700 ? ["LinkedIn: 700 char limit"] : []`). Verify it's correct. If the text says 700, keep it as-is.

    Note: The composer currently has `mediaCount` tracking only if it exists from Phase 2. If `mediaCount` state doesn't exist, reference the multi-image carousel detection using post's `multiImage` property or a local state variable `mediaCount` that increments on media upload. Create a simple `const [mediaCount, setMediaCount] = useState(0)` that updates when media is added.
  </action>
  <acceptance_criteria>
    - LinkedIn accounts are fetched on mount via /api/social-accounts?platform=linkedin
    - Composer shows carousel unsupported notice when 2+ media items added and LinkedIn is relevant (D-07)
    - Existing 700-char LinkedIn caption warning still works (already present)
    - "Publish Now" button is enabled when any account type (FB/IG/LinkedIn) is connected
    - PublishModal receives linkedinAccounts prop
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --strict src/app/compose/new/page.tsx 2>&1</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 6: Tests for LinkedInPublisher</name>
  <read_first>
    - /home/jhpy/Content-Creator/src/lib/publish/instagram.test.ts (unit test pattern — mocked fetch, prepare/publish/verify)
    - /home/jhpy/Content-Creator/src/lib/publish/linkedin.ts (new LinkedInPublisher to test)
  </read_first>
  <action>
    Create `src/lib/publish/linkedin.test.ts` — unit tests for LinkedInPublisher with mocked fetch:

    Structure: `describe("LinkedInPublisher (PUBL-01, PUBL-02)")`

    `describe("prepare")`:
    - `returns ready for valid text-only post` — text:"Valid LinkedIn post", media:[]
    - `returns ready for valid text + single image` — text:"Post", media:[{contentType:"image/jpeg", publicUrl:"https://r2.example.com/photo.jpg"}]
    - `returns ready for valid PNG image` — contentType:"image/png"
    - `returns ready for valid GIF image` — contentType:"image/gif"
    - `rejects empty post` — text:"", media:[]
    - `rejects caption over 700 chars` — text:"x".repeat(701), media:[]
    - `rejects multi-image (carousel)` — media with 2 items
    - `rejects single video` — media with contentType:"video/mp4"
    - `rejects unsupported image format` — contentType:"image/webp"
    - `rejects 3+ media items` — media with 3 items

    `describe("publish")`:
    - `publishes text-only post successfully` — single fetch mock for POST /rest/posts (no image upload), expect success:true
    - `publishes text + single image successfully` — 3 fetch mocks: (1) GET image from publicUrl, (2) POST /rest/images?action=initializeUpload, (3) PUT to uploadUrl, (4) POST /rest/posts
    - `returns error for invalid token` — fetch mock returns 401, expect success:false
    - `returns text-only fallback when image upload fails` — image upload mock returns error, post creation mock succeeds, expect success:true with error:"published (media failed)" (D-03)
    - `returns error when LinkedIn user ID is missing` — no platformAccountId, expect success:false

    `describe("verify")`:
    - `returns published status` — returns {status:"published", platformRef}

    Mock strategy:
    - Use `vi.fn().mockResolvedValueOnce(...)` for sequential fetch calls
    - Mock `global.fetch` with the correct responses for each API call
    - For the image upload flow: mock GET to publicUrl returning a blob, then POST to initializeUpload, then PUT to uploadUrl, then POST to create post
  </action>
  <acceptance_criteria>
    - LinkedInPublisher.prepare() tests cover: valid text-only, valid text+image, empty post, over-700 chars, carousel rejection, video rejection, unsupported format
    - LinkedInPublisher.publish() tests cover: text-only success, text+image success, auth failure, media fallback (D-03), missing user ID
    - LinkedInPublisher.verify() returns published
    - All fetch calls properly mocked with vitest
    - All unit tests pass with mocked fetch
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jhpy/Content-Creator && npx vitest run src/lib/publish/linkedin.test.ts 2>&1</automated>
  </verify>
</task>

</tasks>

<dependency_graph>
- Task 1 (LinkedInPublisher) → Task 2 (factory) depends on Task 1
- Task 2 (factory) → Task 3 (route) can run after Task 2
- Task 4 (PublishModal) independent of Task 1-3 (only needs types from schema)
- Task 5 (compose page) independent of Task 1-3 (only needs to fetch accounts + pass to modal)
- Task 6 (tests) depends on Task 1

Parallelism: Tasks 4 and 5 can run in parallel with Tasks 1-3. Task 6 after Task 1.

Execution order: Task 1 → {Task 2, Task 4, Task 5 in parallel} → Task 3 (after Task 2) → Task 6 (after Task 1)
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client app→API | User selects LinkedIn accounts; POST body arrives from browser |
| API→LinkedIn | Route handler calls LinkedIn REST API with user's decrypted token |
| worker→LinkedIn | BullMQ worker calls LinkedIn REST API with decrypted token from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | POST /rest/images upload URL | mitigate | LinkedIn provides upload URL server-side; we don't construct it. Validate URL is `https://` before PUT. |
| T-06-02 | Tampering | LinkedIn API response | mitigate | Validate response JSON structure before reading fields (try/catch). Do not trust `uploadUrl` or `image` without checking types. |
| T-06-03 | Information Disclosure | Token in error logs | mitigate | Never log `accessToken` or request body containing tokens. LinkedIn errors include `serviceErrorCode` but not raw tokens. |
| T-06-04 | Denial of Service | Rate limits (429) | accept | 429s handled by BullMQ exponential backoff (existing). Advanced rate-limit observability deferred to Phase 7 (D-13). |
| T-06-05 | Elevation of Privilege | Expired LinkedIn tokens | mitigate | PublishModal disables expired-token accounts (D-11). Worker will get HTTP 401 and mark target as failed — no data loss, just graceful degradation. |
| T-06-06 | Repudiation | LinkedIn API failure attribution | mitigate | Raw LinkedIn error (serviceErrorCode) is stored in publishTargets.errorMessage per D-12 for audit trail. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | LinkedInPublisher uses only built-in `fetch` and Node `crypto` — no new dependencies. Existing packages (bullmq, ioredis) were audited in Phase 3. |
</threat_model>

<verification>
- **LinkedIn coverage**: PUBL-01 (immediate publish to LinkedIn), PUBL-02 (LinkedInPublisher adapter), PUBL-03 (per-target tracking via existing pipeline — errorMessage stores raw LinkedIn errors).
- **Pattern compliance**: LinkedInPublisher mirrors MetaPublisher/InstagramPublisher pattern exactly. Factory gating via PUBLISHER_MODE env var.
- **Decisions respected**: D-01 through D-14 as specified in 06-CONTEXT.md.
- **Worker unchanged**: worker.ts processes LinkedIn jobs via getPublisher("linkedin") → LinkedInPublisher.
- **Backward compatible**: Existing Facebook and Instagram publish flow unchanged.
- **No schema changes needed**: social_account platform check already includes 'linkedin'; PublishPlatform type already includes "linkedin".
- **Reconnect UX**: connection-status.ts already handles 60-day expiry; PublishModal surface via red badges + disabled selectors.
</verification>

<success_criteria>
1. Team can publish a text-only post to a connected LinkedIn account via PublishModal — flows through BullMQ worker → LinkedInPublisher → LinkedIn REST API.
2. Team can publish a text + single image (JPEG/PNG/GIF) to LinkedIn via the same pipeline.
3. LinkedIn-specific validation (700 char limit, single image only, JPEG/PNG/GIF format, no carousel, no video) works in prepare() and composer.
4. LinkedIn accounts with expiring tokens show red "Reconnect required" badge and are disabled in the selector; non-expired accounts are selectable normally.
5. Composer shows "LinkedIn does not support carousel posts" notice when 2+ media items are present.
6. Bulletin: PUBLISHER_MODE environment variable now supports all three real platforms (meta, instagram, linkedin).
7. All tests pass: linkedin.test.ts (prepare/publish/verify unit tests with mocked fetch).
</success_criteria>

<output>
Write `.planning/phases/06-publish-to-linkedin/06-01-SUMMARY.md` with tasks executed, tests run, and readiness assessment.
</output>
